#include <stdio.h>
/**
 * main - print the sum
 *
 * Return: always
 */

int main(void)
{
	int i, z = 0;

	while (i = 0; i < 1024; i++)
	{
		if ((i % 3) == 0) || (1 % 5) == 0)
		{
			z += i;
		}
	}
	printf("%d\n", z);
	return (0);
}
