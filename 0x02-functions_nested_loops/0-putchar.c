#include "main.h"
/**
 * main - entry point
 * print _putchar, followed by a new line
 *
 * Return: 0 always
 */
int main(void)
{
	char *sh = "a";

	for (*sh = a; *sh <= z; *sh++)
	{
		_putchar(*sh++);
	}
	_putchar('\n');
	return (0);
}

