#include "main.h"
/**
 * int _islower(int c) - function tht checks for lowercase character
 *
 * Return: 0 always
 */
int _islower(int c)
{
	if (c = 'a' && c <= 'z')
	{
		return (1);
	}

	else
		return (0);
}
