#include "main.h"
/**
 * main - test function thatb prints if integer is postive or negative
 *
 * Return: 0
 */
void positive_or_negative(int i)
{
	
	i = 0;
	positive_or_negative(i);

	return (0);
}
